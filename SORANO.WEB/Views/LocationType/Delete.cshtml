@model LocationTypeModel

@{
    ViewBag.Title = "SORANO - Удаление типа мест";
    ViewBag.Header = "Удаление";
    ViewBag.Description = "типа мест";
    ViewBag.Icon = "fa fa-fw fa-trash";
}

@section Breadcrumb {
    <ul class="breadcrumb">
        <li><a asp-controller="Home" asp-action="Index">Главная</a></li>
        <li><a asp-controller="Location" asp-action="Index">Места</a></li>
        <li class="active">Удаление типа мест</li>
    </ul>
}

<div class="col-lg-6 col-sm-12">
    <div class="row">
        <p class="@(Model.CanBeDeleted ? "text-warning" : "text-danger")">
            <i class="fa fa-fw fa-exclamation" aria-hidden="true"></i>
            @(Model.CanBeDeleted
                ? "Вы уверены, что хотите удалить следующий тип мест?"
                : "Тип мест не может быть удалён, так как у него есть связанные с ним места.")
        </p>
    </div>
    <br />
    <div class="row">
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Name)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Name)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.Description)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Description)
            </dd>
        </dl>
        <form asp-action="Delete">
            <input type="hidden" asp-for="ID" />
            <div class="form-group">
                <a class="btn btn-default btn-sm" asp-controller="Location" asp-action="Index">
                    <i class="fa fa-fw fa-close"></i>
                    Отмена
                </a>
                <button type="submit" class="btn btn-danger btn-sm @(Model.CanBeDeleted ? "" : "disabled")">
                    <i class="fa fa-fw fa-trash"></i>
                    Удалить
                </button>
            </div>
        </form>
    </div>
</div>
